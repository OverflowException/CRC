1. static_cast? reinterpret_cast? Why does casting void* does not need reinterpret_cast? why does converting "uint16_t*" to "unsigned char*" needs reinterpret_cast?
2. std::string container does not work